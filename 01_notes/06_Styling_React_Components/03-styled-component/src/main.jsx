import ReactDOM from 'react-dom/client';

import App from './App.jsx';
import './index.css';  // here we add  vanilla css directly

ReactDOM.createRoot(document.getElementById('root')).render(<App />);
